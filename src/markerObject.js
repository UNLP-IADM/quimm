function MarkerObject(id, title, description, category, confirmacion, lat, lng, mark) {
  this.id = id,
  this.title = title,
  this.description = description,
  this.category = category,
  this.confirmacion = confirmacion,
  this.lat = lat,
  this.lng = lng,
  this.markerLeaflet = mark;
}

